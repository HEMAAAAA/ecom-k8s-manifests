apiVersion: v1
kind: Service
metadata:
  name: mongo-product-catalog-service
  namespace: ecommerce
spec: 
  ports:
    - port: 27017
      targetPort: 27017
      protocol: TCP
  type: ClusterIP
  labels:
    app: mongodb-product-catalog    
  selector:
    app: mongodb-product-catalog
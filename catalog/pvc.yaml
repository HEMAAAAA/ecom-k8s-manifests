apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-product-catalog-pvc
  namespace: ecommerce
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
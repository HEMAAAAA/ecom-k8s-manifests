apiVersion: v1
kind: Service
metadata:
  name: profile-management-service
  namespace: ecommerce
  labels:
    app: profile-management
spec:
  type: ClusterIP
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
  selector:
    app: profile-management
    version: "1.0"